# Archived

This client extensions belonged to the former version of the training.

The map component in this client extension is using Google Maps which requires an API key.

We have replaced this client extension with `clarity-distributor-map-osm-custom-element`
which relies on Open Street Maps instead, providing the same capabilities.

This client extension is not maintained.